.todo
  - if todo.user == current_user 
    You asked #{@account.friend(current_user).name.split[0]} to #{todo.task}, but they have not. 
  - else
    = button_to "✓", account_todo_path(@account, todo), :method => :delete, :class => 'link_button right'
    #{@account.friend(current_user).name.split[0]} wants you to #{todo.task}.
    
