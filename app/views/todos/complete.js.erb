$('<%= "#todo_#{@todo.id}" %>').fadeOut('slow', function() {
  $('<%= "#todo_#{@todo.id}" %>').remove();
  $('<%= escape_javascript(render :partial => 'todos/jstodo', :locals => {:todo => @todo} ) %>').prependTo('#finished');
  $('<%= "#todo_#{@todo.id}" %>').fadeIn('slow');
});

