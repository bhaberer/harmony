.form
  = form_for @todo, :url => account_todos_path(@account) do |f|
    - if @todo.errors.any?
      #errorExplanation
        %h2= "#{pluralize(@todo.errors.count, 'error')} prohibited this user from being saved:"
        %ul
        - @todo.errors.full_messages.each do |msg|
          %li= msg
    = radio_button_tag :type, :me 
    = label_tag :type_me, "I need to do this." 
    = radio_button_tag :type, :you 
    = label_tag :type_me, "#{@account.friend(current_user).name} needs to do this." 
    = radio_button_tag :type, :we 
    = label_tag :type_me, "Both of us." 
    = radio_button_tag :type, :either
    = label_tag :type_me, "Either of us." 

    .field= f.text_field :task
    = f.submit :class => :button
  %br.clear
